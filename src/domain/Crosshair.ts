import type { CrosshairSettings } from "./types";

export const DEFAULT_CROSSHAIR_SETTINGS: CrosshairSettings = {
  color: "#40E0D0",
  lineLength: 8,
  lineThickness: 2,
  centerGap: 4,
  showCenterDot: true,
  centerDotSize: 2,
};
